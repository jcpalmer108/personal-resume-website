import { SkillsProps } from "../types/Skills";
import { Wrapper } from "../styles/components/Skills";

export default function Skills({ content }: SkillsProps) {
  console.log(content)
  return (<Wrapper>Hi there</Wrapper>)

}